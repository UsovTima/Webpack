# Example

```powershell
To start build:

npm i (to start project)

npm run watch   (for start watching the project)
npm run build   (for build project once with minification of .js and .css files)

1. Rename project in gitignore, package-lock, package and in index.html.
2. Let's start work and clear 1-2 paragraph.

```
